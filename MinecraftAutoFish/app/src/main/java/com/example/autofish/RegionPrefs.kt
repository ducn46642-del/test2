package com.example.autofish

object RegionPrefs {
    const val NAME = "autofish_region"
    const val KEY_X = "x"
    const val KEY_Y = "y"
    const val KEY_W = "w"
    const val KEY_H = "h"
    // Vị trí chạm để thả câu / giật cần (mặc định giữa màn hình, nơi có nút bấm trong Minecraft PE)
    const val KEY_TAP_X = "tap_x"
    const val KEY_TAP_Y = "tap_y"
    // Số lần thả cần tối đa cho 1 phiên chạy (0 = không giới hạn)
    const val KEY_MAX_USES = "max_uses"
}
